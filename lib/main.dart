// Default entry point — runs the dev flavor.
// Use main_dev.dart, main_staging.dart, or main_prod.dart via flavor-specific
// build commands (e.g. flutter run --flavor dev --target lib/main_dev.dart).
export 'main_dev.dart';
